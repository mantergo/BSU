source.with.encoding('D:/ВКИАД/lab2/lab2.R', encoding='UTF-8')
sum(abs(d-sr_znach))/n
rm(list=ls(all=TRUE))
dat<-read.table(file="f.txt",sep=",")
d<-as.numeric(dat)
sr_zn<-mean(d)    #среднее значение
disp<-var(d)  #дисперсия
View(d)
kv_o<-sd(d)    #квадратическое отклонение
moda<-which.max(table(d)) #мода
med<-median(d)  #медиана
#коэффициент ассиметрии
a<-table(d)
a
zn<-sort(unique(d))
zn
sr_znach<-mean(d)
n<-length(d)
n
disp<-var(d)
sr_znach
disp
koeff_asimm<-sum((zn-sr_znach)^3*a)/(n*disp^3)
koeff_asimm
koeff_ex
koeff_ex<-sum((zn-sr_znach)^4*a)/(n*disp^4)-3
p<-(m/length(d))
m<-5
p
b<-sort(d)
b
usech_sr<-mean(d,p,TRUE)
usech_sr
sum(abs(d-sr_znach))/n
koeff_ex<-sum((zn-sr_znach)^4*a)/(n*disp^4)-3
koeff_ex
m<-5
p<-(m/length(d))
p
b<-sort(d)
b
usech_sr<-mean(d,p,TRUE)
usech_sr
sum(abs(d-sr_znach))/n
d
d-sr_znach
sum(abs(d-sr_znach))
abs(d-sr_znach)
sum(abs(d-sr_znach))
sum(abs(d-sr_znach))/n
lin_otkl<-sum(abs(d-sr_znach))/n
source.with.encoding('D:/ВКИАД/lab2/lab2.R', encoding='UTF-8')
f<-table(1,5)
source.with.encoding('D:/ВКИАД/lab2/lab2.R', encoding='UTF-8')
source.with.encoding('D:/ВКИАД/lab2/lab2.R', encoding='UTF-8')
f<-table(0,1,5)
f<-table(0,5,5)
f
f<-table(0)
f
?table
?table
source.with.encoding('D:/ВКИАД/lab2/lab2.R', encoding='UTF-8')
?table
help(table)
source.with.encoding('D:/ВКИАД/lab2/lab2.R', encoding='UTF-8')
source.with.encoding('D:/ВКИАД/lab2/lab2.R', encoding='UTF-8')
rm(list=ls(all=TRUE))
dat<-read.table(file="f.txt",sep=",")
d<-as.numeric(dat)
#View(d)
sr_zn<-mean(d)    #среднее значение
disp<-var(d)  #дисперсия
kv_o<-sd(d)    #квадратическое отклонение
moda<-which.max(table(d)) #мода
med<-median(d)  #медиана
#коэффициент ассиметрии
a<-table(d)
a
zn<-sort(unique(d))
zn
sr_znach<-mean(d)
sr_znach
n<-length(d)
n
disp<-var(d)
disp
koeff_asimm<-sum((zn-sr_znach)^3*a)/(n*disp^3)
koeff_asimm
koeff_ex<-sum((zn-sr_znach)^4*a)/(n*disp^4)-3
koeff_ex
m<-5
p<-(m/length(d))
p
b<-sort(d)
b
#Усеченное среднее
usech_sr<-mean(d,p,TRUE)
usech_sr
#Коэффициент вариации
koef_var<-disp/sr_znach*100
koef_var
koef_var
lin_otkl<-sum(abs(d-sr_znach))/n
#Линейное отклонение
lin_otkl
koeff_asimm<-sum((d-sr_znach)^3)/(disp^3)
koeff_asimm
source.with.encoding('D:/ВКИАД/lab2/lab2.R', encoding='UTF-8')
rm(list=ls(all=TRUE))
dat<-read.table(file="f.txt",sep=",")
d<-as.numeric(dat)
#View(d)
sr_zn<-mean(d)    #среднее значение
disp<-var(d)  #дисперсия
kv_o<-sd(d)    #квадратическое отклонение
moda<-which.max(table(d)) #мода
med<-median(d)  #медиана
#коэффициент ассиметрии
a<-table(d)
a
zn<-sort(unique(d))
zn
sr_znach<-mean(d)
sr_znach
n<-length(d)
n
disp<-var(d)
disp
koeff_asimm<-sum((zn-sr_znach)^3*a)/(n*disp^3)
koeff_asimm
koeff_ex<-sum((zn-sr_znach)^4*a)/(n*disp^4)-3
koeff_ex
m<-5
p<-(m/length(d))
p
b<-sort(d)
b
#Усеченное среднее
usech_sr<-mean(d,p,TRUE)
usech_sr
#Коэффициент вариации
koef_var<-disp/sr_znach*100
koef_var
#Линейное отклонение
lin_otkl<-sum(abs(d-sr_znach))/n
lin_otkl
source.with.encoding('D:/ВКИАД/lab2/lab2.R', encoding='UTF-8')
m<-10
p<-(m/length(d))
b<-sort(d)
b
p
#Усеченное среднее
usech_sr<-mean(d,p,TRUE)
usech_sr
rm(list=ls(all=TRUE))
dat<-read.table(file="f.txt",sep=",")
d<-as.numeric(dat)
#View(d)
sr_zn<-mean(d)    #среднее значение
disp<-var(d)  #дисперсия
kv_o<-sd(d)    #квадратическое отклонение
moda<-which.max(table(d)) #мода
med<-median(d)  #медиана
#коэффициент ассиметрии
a<-table(d)
a
zn<-sort(unique(d))
zn
sr_znach<-mean(d)
sr_znach
n<-length(d)
n
disp<-var(d)
disp
koeff_asimm<-sum((zn-sr_znach)^3*a)/(n*disp^3)
koeff_asimm
koeff_ex<-sum((zn-sr_znach)^4*a)/(n*disp^4)-3
koeff_ex
m<-10
p<-(m/length(d))
p
b<-sort(d)
b
#Усеченное среднее
usech_sr<-mean(d,p,TRUE)
usech_sr
#Коэффициент вариации
koef_var<-disp/sr_znach*100
koef_var
#Линейное отклонение
lin_otkl<-sum(abs(d-sr_znach))/n
lin_otkl
sr_zn<-mean(d)    #среднее значение
disp<-var(d)  #дисперсия
kv_o<-sd(d)    #квадратическое отклонение
moda<-which.max(table(d)) #мода
med<-median(d)  #медиана
#коэффициент ассиметрии
a<-table(d)
a
zn<-sort(unique(d))
zn
sr_znach<-mean(d)
sr_znach
n<-length(d)
n
disp<-var(d)
disp
koeff_asimm<-sum((zn-sr_znach)^3*a)/(n*disp^3)
koeff_asimm
koeff_ex<-sum((zn-sr_znach)^4*a)/(n*disp^4)-3
koeff_ex
m<-10
p<-(m/length(d))
p
b<-sort(d)
b
#Усеченное среднее
usech_sr<-mean(d,p,TRUE)
usech_sr
#Коэффициент вариации
koef_var<-disp/sr_znach*100
koef_var
#Линейное отклонение
lin_otkl<-sum(abs(d-sr_znach))/n
lin_otkl
rm(list=ls(all=TRUE))
dat<-read.table(file="f.txt",sep=",")
d<-as.numeric(dat)
#View(d)
sr_zn<-mean(d)    #среднее значение
disp<-var(d)  #дисперсия
kv_o<-sd(d)    #квадратическое отклонение
moda<-which.max(table(d)) #мода
med<-median(d)  #медиана
#коэффициент ассиметрии
a<-table(d)
a
zn<-sort(unique(d))
zn
sr_znach<-mean(d)
sr_znach
n<-length(d)
n
disp<-var(d)
disp
koeff_asimm<-sum((zn-sr_znach)^3*a)/(n*disp^3)
koeff_asimm
koeff_ex<-sum((zn-sr_znach)^4*a)/(n*disp^4)-3
koeff_ex
m<-10
p<-(m/length(d))
p
b<-sort(d)
b
#Усеченное среднее
usech_sr<-mean(d,p,TRUE)
usech_sr
#Коэффициент вариации
koef_var<-disp/sr_znach*100
koef_var
#Линейное отклонение
lin_otkl<-sum(abs(d-sr_znach))/n
lin_otkl
